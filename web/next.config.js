/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable static export for simpler deployment if needed
  // output: 'export',
}

module.exports = nextConfig
